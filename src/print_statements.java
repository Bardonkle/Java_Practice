// main is always used in java to execute a command
// every Java program has a class name which must match the filename, and that every program must contain the main() method.
// println is short for print line which prints on a new line
// print shows on the same line instead

public class print_statements {
    public static void main(String[] args) throws Exception {
        System.out.println("Hello, World!");
    }
}
